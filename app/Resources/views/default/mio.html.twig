{% extends 'base.html.twig' %}

{% block body %}
prova form
    {{ print_r(phpinfo()) }}
    {{ form_start(form) }}
    {{ form_widget(form) }}

    <input type="submit" value="Create" class="btn btn-default pull-right" />
    {{ form_end(form) }}

{% endblock %}